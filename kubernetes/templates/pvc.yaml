apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "omegasync.fullname" . }}
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 100Gi
  storageClassName: omegasync-sc
  selector:
    matchLabels:
      app: "omegasync"